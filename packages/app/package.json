{
  "name": "@md-task/app",
  "version": "0.1.0",
  "description": "MdTask GUI shell. Vite frontend now; Tauri wrapper later (needs Rust toolchain).",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "npm run build -w @md-task/core && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@md-task/core": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
