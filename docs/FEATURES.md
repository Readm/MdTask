# MdTask Feature List

> 状态：v0.2 功能确认完成（2026-08-14）。优先级：P0 = v0.2 Obsidian 插件 MVP 必做、
> P1 = v1.0（桌面/手机 app）、P2 = 后置或可选。确认列：✓ = 已确认，[ ] = 待定。

## A. 协议层

| 编号 | 功能 | 优先级 | 确认 | 备注 |
|------|------|--------|------|------|
| A1 | 协议文档 docs/protocol.md：任务行定义、缩进派生父子、边界规则、UTF-8、行尾兼容 | P0 | ✓ | "协议+多应用"的宪法 |
| A2 | 任务模型：行号+缩进层级+checkbox 状态+原样文本；父子勾选**不联动** | P0 | ✓ | 勾哪个是哪个 |
| A3 | 解析边界：核心解析器纯行级规则（不感知代码块）；Obsidian 壳层借助编辑器 API 感知代码块 | P0 | ✓ | 协议保持最简 |
| A4 | 原地回写原语：toggle/indent/outdent/insert/move/delete，只改目标行 | P0 | ✓ | 其他内容字节级不动 |
| A5 | **折叠状态入文件**：父任务行尾 ` ▼` = 折叠，展开无标记；解析器剥离标记入 collapsed 字段；编辑原语保留标记 | P0 | ✓ | 用户决策：写文件，非 UI 状态（修改自初案） |
| A6 | 扩展口子声明：文档注明未来属性语法（日期/优先级）预留区，v0.2 不实现 | P2 | ✓ | |
| A7 | 兼容性声明：与 obsidian-tasks 单向兼容（其读取器可解析我们的文件） | P1 | ✓ | 折叠 ▼ 被其视为普通文本，无害 |

## B. 核心库 @md-task/core

| 编号 | 功能 | 优先级 | 确认 | 备注 |
|------|------|--------|------|------|
| B1 | 解析器：文本 → 树（实现 A2/A3/A5） | P0 | ✓ | |
| B2 | 编辑原语：A4 全部纯函数实现 | P0 | ✓ | 输入文本 → 输出新文本 |
| B3 | 变更检测：文件 hash + mtime 对比（冲突提示基础） | P1 | ✓ | |
| B4 | 测试覆盖：解析+原语全路径（toggle 边界、深层缩进、空文件、CRLF、折叠标记） | P0 | ✓ | |
| B5 | 性能目标：10MB 文件解析 < 200ms，编辑原语 < 5ms | P0 | ✓ | |

## C. Obsidian 插件（v0.2 MVP）

| 编号 | 功能 | 优先级 | 确认 | 备注 |
|------|------|--------|------|------|
| C1 | 插件骨架：manifest、main.ts、命令注册、设置页、esbuild | P0 | ✓ | |
| C2 | Tab = 缩进为上一任务的子任务；Shift+Tab = 提升层级；**去掉 Alt+Tab**；CodeMirror 6 键位定制，仅列表上下文生效 | P0 | ✓ | Alt+Tab 与系统切窗冲突 |
| C3 | 勾选快捷键：Cmd/Ctrl+Enter 或 Cmd/Ctrl+Click，行内任意位置可触发 | P0 | ✓ | |
| C4 | 折叠/展开：点击 ▼ 标记折叠子树，状态写回文件（A5）；视图层隐藏子任务 | P0 | ✓ | |
| C5 | 树视图面板：当前文件任务树、点击勾选回写、展开/折叠、已完成保留原位 | P0 | ✓ | |
| C6 | 命令面板：全部操作注册为 Obsidian 命令 | P0 | ✓ | |
| C7 | 设置页：最小设置（"完成淡化"开关等） | P1 | ✓ | 允许已完成任务淡化显示（默认开），不违背保留原则 |
| C8 | 树视图已完成任务视觉：淡化处理 | P1 | ✓ | 与 C7 联动 |

## D. 桌面 app（v1.0，Tauri）

| 编号 | 功能 | 优先级 | 确认 | 备注 |
|------|------|--------|------|------|
| D1 | Tauri 壳 + 原生文件系统访问 | P1 | [ ] | |
| D2 | 文件夹模式：打开 markdown 文件夹，多文件浏览 | P1 | [ ] | |
| D3 | 树编辑器：复用 core，键盘优先 | P1 | [ ] | 手感对齐 C2/C3 |
| D4 | 文件监听热刷新 | P1 | [ ] | F3 |
| D5 | 轻量冲突提示（hash/mtime） | P1 | [ ] | F3 |
| D6 | 多文件聚合视图（跨文件任务汇总） | P2 | [ ] | 后置 |

## E. 手机 app（v1.0）

| 编号 | 功能 | 优先级 | 确认 | 备注 |
|------|------|--------|------|------|
| E1 | 触屏树编辑：长按拖拽调层级、点击勾选、折叠 | P1 | [ ] | F2：完整编辑 |
| E2 | 文件夹访问：iOS 文件 provider / Android SAF | P1 | [ ] | |
| E3 | 打开时重载兜底（iOS 后台监听受限） | P1 | [ ] | F3 已认可 |
| E4 | 离线可用：文件本地化，天然离线 | P1 | [ ] | |

## F. 工程与发布

| 编号 | 功能 | 优先级 | 确认 | 备注 |
|------|------|--------|------|------|
| F1 | CI：core 测试 + 插件构建 | P0 | ✓ | 骨架已有，扩展 |
| F2 | README 中英双语 | P0 | ✓ | E2 |
| F3 | 致谢 obsidian-tasks（MIT） | P0 | ✓ | S2 |
| F4 | 协议文档独立发布（A1 产物可被其他实现引用） | P1 | [ ] | |
| F5 | 插件上架 Obsidian 社区市场（obsidian-releases review 流程） | P2 | [ ] | v0.2 稳定后 |
| F6 | CHANGELOG + semver | P1 | [ ] | |

## 待确认汇总

- D 组（桌面 app）5 项、E 组（手机 app）4 项、F4/F6：v1.0 阶段确认，暂不阻塞 v0.2
- F5：v0.2 稳定后再定
